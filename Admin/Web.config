<?xml version="1.0"?>
<configuration>
    <system.web>
      <authorization>
        <allow roles="Admin" /> <!-- allow access only to Admin user of the system -->
        <deny users="*" /> 
      </authorization>
    </system.web>
</configuration>
